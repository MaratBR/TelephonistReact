# Generated by confd {{datetime}}
# Default configuration for Telephonist proxy server


server {
    listen       80;
    listen  [::]:80;
    server_name  {{getv "/nginx/server/name"}};

{{if exists "/ssl"}}
    ssl_certificate     {{getv "/ssl/certificate"}};
    ssl_certificate_key {{getv "/ssl/key"}};
    ssl_protocols       {{getv "/ssl/protocols" "TLSv1 TLSv1.1 TLSv1.2"}};
    ssl_ciphers         {{getv "/ssl/ciphers" "HIGH:!aNULL:!MD5"}};
{{end}}

    #location /api/v1/_ws/ {
	#	proxy_http_version 1.1;
#		proxy_set_header Host $http_host;
#		proxy_set_header X-Real-IP        $remote_addr;
#		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#		proxy_set_header X-Forwarded-Proto $scheme;
#
#		proxy_redirect off;
#		proxy_buffering off;
#		keepalive_timeout 90s;
#		proxy_connect_timeout 10s;
#		proxy_read_timeout 30s;
#		proxy_send_timeout 30s;
#		proxy_set_header Upgrade $http_upgrade;
#		proxy_set_header Connection "Upgrade";
#
 #   {{if exists "/api/usessl"}}
  #      proxy_pass https://api-upstream/_ws/;
   # {{else}}
    #    proxy_pass http://api-upstream/_ws/;
#    {{end}}
#	}

#	location /api/v1/ {
#		proxy_http_version 1.1;
#		proxy_set_header Host $http_host;
#		proxy_set_header X-Real-IP        $remote_addr;
#		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#		proxy_set_header X-Forwarded-Proto $scheme;
#
#		if ($request_method = OPTIONS ) {
#			return 200;
#		}
#
 #   {{if exists "/api/usessl"}}
#        proxy_pass httdops://api-upstream/;
#    {{else}}
#        proxy_pass http://api-upstream/;
#    {{end}}
#    }
    
    location / {
        root   /dist;
        try_files $uri $uri/ /index.html;
    }
}

